import 'tables.moor';

_userProfile: SELECT * FROM userProfiles WHERE id = 1;
_timestamp: SELECT CAST(strftime('%s', 'now', 'localtime') AS INT) * 1000 AS timestamp;
_recurrenceById: SELECT * FROM recurrenceTypes WHERE id=:id;
