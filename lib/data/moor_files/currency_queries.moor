import 'tables.moor';

_allCurrencies: SELECT * FROM currencies ORDER BY abbrev ASC;
_currencyById: SELECT * FROM currencies WHERE id=:id;
_userCurrency: SELECT currencies.* FROM currencies, userProfiles WHERE currencies.id = userProfiles.currencyId;
