import 'tables.moor';

allMonths: SELECT * FROM months ORDER BY firstDate ASC;
monthIdByDate(:date AS DATE): SELECT id FROM months WHERE :date <= lastDate AND :date >= firstDate;
monthByDate(:date AS DATE): SELECT * FROM months WHERE :date <= lastDate AND :date >= firstDate;
monthById: SELECT * FROM months WHERE id=:id;
